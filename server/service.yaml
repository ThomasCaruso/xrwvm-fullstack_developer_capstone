apiVersion: v1
kind: Service
metadata:
  name: dealership
  labels:
    app: dealership
spec:
  type: NodePort
  selector:
    app: dealership
  ports:
    - name: http
      port: 8000
      targetPort: http
      protocol: TCP
